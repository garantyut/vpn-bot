services:
  vpn_bot:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./:/app
    env_file:
      - .env
    command: bash -lc "pip install -r requirements.txt && python -m src.bot"
    restart: unless-stopped
